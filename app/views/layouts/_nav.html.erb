<nav class="navbar navbar-default navbar-fixed-top" style="margin-bottom: 0px; background-color: rgb(240,240,240); ">
       <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <%= link_to "",hotels_home_path,class: "navbar-brand topnav",style:'background:url(/images/logo.jpeg);color:Blue' %>
        </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse pull-left navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                <li>
                    <%= link_to "Home",hotels_home_path,class: "navbar-brand topnav" %>
                </li>
                <li>
                    <% if current_user.present? %>
                    <% if current_user.profile.present? %>
                    <%= link_to "Profile",profile_path(current_user.id),class: "navbar-brand topnav"%>
                    <% else %>
                    <%= link_to "Create Profile",new_profile_path,class: "navbar-brand topnav"%>
                    <% end %>
                    <% end %>
                </li>
                <li>
                    <%= link_to "Rooms",rooms_path,class: "navbar-brand topnav" %>
                </li>
                <li>
                    <%= link_to "Reviews",reviews_index_path,class: "navbar-brand topnav" %>
                </li>
                <li>
                  <% if current_user.present? %>
                  <% if current_user.is_admin? %>
                  <%= link_to "Users",users_index_path,class: "navbar-brand topnav" %>
                  <% end %>
                  <% end %>
              </li>
              <li>
                <% if current_user.present? %>
                <%= link_to "Sign Out",destroy_user_session_path, method: 'delete',class: "navbar-brand topnav"  %>
                <% else %>
                <%= link_to "Sign In",new_user_session_path,class: "navbar-brand topnav"  %>
                <% end %>
            </li>
            
        </ul>
    </div>
    <!-- /.navbar-collapse -->
</div>
<!-- /.container -->
</nav>